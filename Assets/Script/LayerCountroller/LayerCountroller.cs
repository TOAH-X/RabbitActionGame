using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LayerCountroller : MonoBehaviour
{
    /*
     * レイヤーメモ(まだ反映していない)
     * 
     * 下層から順に
     * 7:Background
     * 6:StageBase(もしかしたら使うかも)
     * 5:Enemy
     * 4:Player
     * 3:Stage(wallなど)
     * 2:UI(HPバーやキャラアイコン)
     * 1:TextUI(テキストやボタン関連)
     * 0:GachaBase(ガチャやマニュアル画面などベース画面)
     * -1:
     * -2:
     * -3:
     * 
     * 
     */

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
